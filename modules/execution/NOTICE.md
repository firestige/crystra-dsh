# Source notice

This internal module contains Crystra-owned DSH adapter code migrated from Intake and earlier DSH surfaces. It contains no copied Execution domain implementation. The former embedded Workspace UI composition fork has been removed; the native Workspace UI remains an external DSH runtime component.
